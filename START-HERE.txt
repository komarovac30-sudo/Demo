VELOURA V8 — START HERE

If your database already has V7 installed:

A) OPTIONAL DEMO DATA REFRESH
   Supabase -> SQL Editor -> run:
   supabase/seed-demo-v8.sql

   This adds the polished fictional Sienna Vale demo and 20 original fictional verified reviews.
   No V8 schema migration is required.

B) DEPLOY
   1. Extract this ZIP.
   2. Copy/replace all files in your local Demo repository.
   3. Review changes in GitHub Desktop.
   4. Commit to main.
   5. Push origin.
   6. Wait for Vercel auto-deploy.

C) VERIFY
   /u/creator
   /dashboard
   /dashboard/visitors
   /admin
   /admin/creators
   /admin/reviews
   /admin/visitors

KEY V8 CHECKS
- Skeleton loading appears instead of a blank/loader-only page.
- Public profile uses Gallery / Client Reviews / About Me language.
- Hero shows Discreet / Refined / Private tags.
- Profile total likes are labeled Admirers.
- Private Collection language appears consistently.
- 20 demo reviews appear after running seed-demo-v8.sql.
- Reviews display "Verified Review".
- Creator/Admin review verification flow from V7 still works.

IMPORTANT
The seeded profile/reviews are fictional demo content. Do not present them as real third-party reviews.
